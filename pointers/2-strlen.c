#include "main.h"

/**
 * _strlen - counts the number of characters in a string
 *
 * @s: pointer to the character pointer *str in main
 *
 * Return: Total number of characters
 */

int _strlen(char *s)
{
	int i = *s;

	for (i = 0; s[i] != '\0'; i++)
	{

	}

	return (i);
}
